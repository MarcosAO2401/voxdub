[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "voxdub"
version = "0.1.0"
description = "Local video translator/dubber (Fase 1: transcribe)"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["voxdub", "voxdub.pipeline"]
package-dir = {"" = "src/backend"}
